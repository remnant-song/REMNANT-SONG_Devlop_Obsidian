
---

> *2026-08-20 23:04:07*

---

## 重构总结

| 删减 | 新增 |
|------|------|
| `ParticleField` 粒子组件 | 无 |
| `DecorationItem` 装饰组件 + 14 张装饰素材导入 | 无 |
| `layerMid` 中景层 + `layerFront` 前景层 | 无 |
| `MID_MOVE` / `FRONT_MOVE` 视差系数 | 无 |
| `decorations` 数组 | 无 |
| 所有 `midToX/Y` / `frontToX/Y` GSAP 实例 | 无 |

**重构后结构：**

```
AnimeRoom.vue
├── 背景层 (layerBg)    ← bgImg_v3.png，视差 BG_MOVE=10px
└── 物件层 (layerItems) ← bilibili_mirror / github / douyin，视差 ITEMS_MOVE=40px
```

从原来的 ~240 行精简到 ~140 行，移除了 6 个无用 ref、4 个 GSAP 实例、2 个 computed、16 个无用 import、1 个组件引用，代码量减少约 40%。

请审核 diff 后应用。

---

> *2026-08-20 23:20:33*

问题很清晰：当前 `header` 是 `sticky` + `bg-bg/80` 白色背景，占用了 `h-16` 的视觉空间，导致背景图被截断在导航栏下方。需要改为 `fixed` 透明叠加。

---

## 总结

| 文件 | 改前 | 改后 |
|------|------|------|
| `DefaultLayout.vue` header | `sticky top-0` + `bg-bg/80` + `border-b` | `fixed top-0 left-0 right-0` + `bg-transparent` |
| `DefaultLayout.vue` main | `flex-1`（分配剩余空间） | `min-h-screen`（全视口） |
| `DefaultLayout.vue` root | `flex flex-col min-h-screen` | `min-h-screen` |
| `AnimeRoom.vue` | `calc(100vh - 4rem)` | `100vh` |

**效果：**

```
┌─────────────────────────────┐
│  🏠 首页  📝 博客  🚪 传送门  │ ← fixed 透明导航栏，悬浮在背景之上
│                             │
│                             │
│        背景大图/流体         │
│        占据整个屏幕          │
│        (100vh)              │
│                             │
│                             │
└─────────────────────────────┘
```

导航栏不再占用空间分配，背景延伸至整个视口。请审核两个 diff 后应用。